<%= simple_form_for @video do |f| %>
<%= f.input :name %>
<%= f.input :category %>
<%= f.input :link, as: :text, error: "A link is required" %>
<% if @video.link.present? %>
	<<%= @video.link.html_safe %>>
	<br />
<% end %>
<%= f.submit(@video.new_record? ? "Add Video" : "Edit Video", class: "btn btn-success") %>
<% end %>




